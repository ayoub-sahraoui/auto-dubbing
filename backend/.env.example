# Whisper Model Configuration
# Available models: tiny, base, small, medium, large-v3, large-v3-turbo
# Recommended: large-v3-turbo (best balance of speed and accuracy for French)
WHISPER_MODEL=large-v3-turbo

# Device selection: auto (auto-detect GPU), cpu, cuda
WHISPER_DEVICE=auto

# Compute type: auto (auto-select based on device), int8, float16, float32
# auto will use: float16 for GPU, int8 for CPU
WHISPER_COMPUTE_TYPE=auto

# Kokoro TTS Configuration
KOKORO_DEFAULT_VOICE=af_heart
KOKORO_DEFAULT_LANG=a

# Redis Configuration (for future background tasks)
REDIS_URL=redis://localhost:6379/0

# CORS Configuration
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
